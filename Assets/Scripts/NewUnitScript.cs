using UnityEngine;
using UnityEngine.AI;

public class NewUnitScript : MonoBehaviour
{

    // public UnitData unitData;

    // [Header("General")]
    // [SerializeField] public string unitName;
    // [TextArea] [SerializeField] public string description;
    // [SerializeField] public Sprite UnitPortrait;
    // [SerializeField] public UnitFaction faction;
    // [SerializeField] public int playerOwnerID;

    // [Header("Unit Pathing")]
    // [SerializeField] NavMeshAgent agent { get; set; }

    // [Header("Stats")]
    // [SerializeField] int maxHealth { get; set; }
    // [SerializeField] int maxEnergy { get; set; }
    // [SerializeField] float moveSpeed { get; set; }
    // [SerializeField] float fogSightRange { get; set; }

    // [Header("Combat")]
    // [SerializeField] float attackRange { get; set; }
    // [SerializeField] int attackDamage { get; set; }
    // [SerializeField] float attacksPerSecond { get; set; } //Measured in attacks per second
    // [SerializeField] int currentEnergy { get; set; }
    // [SerializeField] int currentHealth { get; set; }

    // [Header("Gathering")]
    // [SerializeField] float harvestingSpeed { get; set; }
    // [SerializeField] int MaxInventory { get; set; }
    // [SerializeField] int currentInventory { get; set; }
    // [SerializeField] ResourceTile currentHarvestTile { get; set; }

    

    
    // [SerializeField] GameObject selectionIndicatorSprite; //Sprite that appears underneath unit to represent it being highlighted

    // public UnitState currentState = UnitState.Idle;

    // private Coroutine harvestingRoutine;

    // [SerializeField] protected Coroutine attackRoutine;
    // [SerializeField] protected Unit attackTarget;

    

    //[Header("Inventory")]
    
    //public Dictionary<ResourceType, int> carriedResources = new Dictionary<ResourceType, int>();
    
    /*
    [Header("Harvest Progress UI")]
    [HideInInspector] public Canvas harvestBarCanvas;
    [HideInInspector] public Slider harvestBar;
    [HideInInspector] public UnitPanelScript UnitPanelScript;
    [HideInInspector] public Animator animator;
    */


    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
